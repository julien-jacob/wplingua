{
  "name": "wplingua",
  "version": "2.0.0",
  "description": "Gulp configuration for wpLingua WordPress plugin",
  "main": "gulpfile.js",
  "scripts": {
    "archive": "gulp clear || npm run archive-windows",
    "archive-windows": "gulp folder_create && npm run zip-windows && gulp folder_delete",
    "zip-windows": "powershell Compress-Archive wplingua wplingua.zip",
    "archive-linux": "gulp folder_create && npm run zip-linux && gulp folder_delete",
    "zip-linux": "zip -r wplingua wplingua.zip"
  },
  "author": "wpLingua Team",
  "dependencies": {
    "gulp": "^5.0.0",
    "gulp-clean": "^0.4.0",
    "gulp-rename": "^2.0.0",
    "gulp-sass": "^5.1.0",
    "gulp-uglify": "^3.0.2",
    "gulp-zip": "^6.0.0",
    "sass": "^1.72.0"
  }
}
